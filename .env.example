# OpenAI API Key
OPENAI_API_KEY=sk-your-api-key-here

# 任意: 文字起こし設定（未設定なら既定値）
# TRANSCRIBE_MODEL=gpt-transcribe   # whisper-1 / gpt-4o-transcribe も可
# TRANSCRIBE_LANGUAGE=ja                # 自動判定にしたければ auto
# TRANSCRIBE_PROMPT=                 # 未設定ならモデル別の既定文（main.py 参照）
# TRANSCRIBE_PREV_TAIL_CHARS=60
# FFMPEG_TIMEOUT_SEC=600
# POLISH_MODEL=gpt-5.4-mini           # LLM 後処理のモデル（UI のチェックで有効/無効）

# 利用履歴（Supabase。秘密鍵はブラウザへ渡さない）
SUPABASE_URL=https://cqsktjmkjkvdkoizolgh.supabase.co
SUPABASE_SECRET_KEY=sb_secret_your-secret-key-here
HISTORY_PASSWORD=change-this-password
# LOG_INCLUDE_FILENAME=false          # true の場合だけ元ファイル名を保存
# TRANSCRIPTION_PRICE_PER_MINUTE=     # 未登録モデルの単価を上書きする場合
